import{test,expect,loadAndMeasure,demoLogin}from"./fixtures";
const routes=["/dashboard","/dashboard/integrations","/dashboard/connections","/dashboard/authorization","/dashboard/policies","/dashboard/approvals","/dashboard/audit","/dashboard/reliability","/dashboard/servers","/dashboard/tools","/dashboard/sdk","/dashboard/bundles","/dashboard/generate","/dashboard/analytics","/dashboard/usage","/dashboard/billing","/dashboard/settings","/dashboard/organization","/dashboard/notifications","/dashboard/reference-integrations/github","/dashboard/reference-integrations/static-skill"];
test.beforeEach(async({page})=>demoLogin(page));
test("dashboard routes render, switch pages, and stay within navigation budget",async({page})=>{const timings:Record<string,number>={};for(const route of routes){timings[route]=await loadAndMeasure(page,route,10_000);await expect(page.locator("body")).toBeVisible();}test.info().annotations.push({type:"navigation-timings",description:JSON.stringify(timings)});});
test("dashboard navigation changes route without a full-page failure",async({page})=>{await page.goto("/dashboard");await page.getByRole("button",{name:"Build"}).click();const link=page.locator('a[href="/dashboard/integrations"]').first();await expect(link).toBeVisible();await link.click();await page.waitForURL("**/dashboard/integrations");await expect(page.getByRole("heading",{name:"Integrations"})).toBeVisible();});
