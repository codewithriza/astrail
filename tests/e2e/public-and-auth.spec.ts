import{test,expect,loadAndMeasure}from"./fixtures";
const pages=["/","/docs","/marketplace","/mcp","/tutorials","/blog","/status","/privacy","/terms","/get-demo","/login","/signup"];
test("public pages load without browser errors and within budget",async({page})=>{for(const path of pages){await loadAndMeasure(page,path);await expect(page.locator("body")).not.toBeEmpty();}});
test("demo email login performs API callback and opens dashboard",async({page})=>{await page.goto("/login");await page.getByLabel("Email").fill("browser-test@astrail.local");await page.getByRole("button",{name:"Sign in"}).click();await page.waitForURL(/\/dashboard/);await expect(page.getByText(/Control plane|Dashboard|Workspace/i).first()).toBeVisible();});
test("open-source landing page links to the code and documentation",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/Your APIs/})).toBeVisible();await expect(page.getByRole("link",{name:/Get the code/})).toHaveAttribute("href","https://github.com/codewithriza/astrail");await expect(page.getByRole("link",{name:/Read the docs/})).toHaveAttribute("href","/docs");await expect(page.getByText("No upstream calls executed.")).toBeVisible();});
