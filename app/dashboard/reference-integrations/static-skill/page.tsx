import{PageFrame}from"@/components/control-plane/PageFrame";
export default function StaticSkillPage(){return <PageFrame eyebrow="Wonsuk evaluation" title="Static skill → live GitHub tools" description="A bounded SKILL.md manifest becomes mapped tools while local context stays local and Astrail handles task authorization, OAuth, provider execution, and audit."><section className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Boundary</h2><ol className="mt-3 list-decimal space-y-2 pl-5 text-sm"><li>Static instructions and safe tool manifest: repository SKILL.md.</li><li>Prompts, memory, and evaluation context: local agent only.</li><li>Hosted boundary: API key, short-lived task authorization, approval policy, OAuth scope checks.</li><li>Execution: deterministic mapped GitHub API calls with audit evidence.</li></ol><p className="mt-4 text-sm">Start with <code>POST /api/reference-integrations/static-skill</code> using the same body as the GitHub reference setup, then complete the returned OAuth URL.</p></section></PageFrame>}
